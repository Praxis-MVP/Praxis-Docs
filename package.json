{
  "name": "praxis-docs",
  "version": "1.0.0",
  "description": "Documentation site built with Markdoc",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js --watch",
    "serve": "npx serve dist"
  },
  "dependencies": {
    "@markdoc/markdoc": "^0.4.0"
  },
  "devDependencies": {
    "chokidar": "^3.5.3",
    "gray-matter": "^4.0.3"
  }
}
